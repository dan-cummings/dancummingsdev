---
import AboutMe from '../layouts/AboutMe.astro';
import { getCollection, render } from 'astro:content';

const collection = await getCollection('pages');
const aboutMe = collection[0];
---

<AboutMe {...aboutMe.data}></AboutMe>
